# webserver_42
A lightweight HTTP server written in C++98, built from scratch using non-blocking I/O and poll(). Supports multiple clients, basic HTTP methods (GET, POST, DELETE), static file serving, CGI execution, and configurable routing inspired by NGINX.
