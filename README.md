## Instructions

### 1. Setup
To configure the environment and add a shortcut for running the server, execute the setup script:
```bash

chmod +x alias.sh
./alias.sh
source ~/.zshrc  # or source ~/.bashrc depending on your shell
